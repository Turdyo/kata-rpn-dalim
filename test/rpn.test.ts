import { expect } from 'chai';

describe('RPN', function () {
    describe('some category', function () {
        it('some test', function () {
            expect(1).to.equal(1);
        });
    });
});
